class InventoryDataModel {
  String id, firebaseId, model, name, description, vendor, country, items,vendorId;

  InventoryDataModel(this.id, this.firebaseId, this.model, this.name,
      this.description, this.vendor, this.country, this.items,this.vendorId);
}
